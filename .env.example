# Optional. The Studio and API can start without it, but generation is disabled.
ARK_API_KEY=

# Optional Ark-compatible endpoint override.
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# Internal maintainers only: inject local Lottery QA fixtures.
# VITE_PET_LORD_INTERNAL_FIXTURES=1
