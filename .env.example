# Provider credentials are configured interactively in Studio and stored locally.
# No generation token is required at process startup.

# Optional. Keep loopback for local use; put remote deployments behind HTTPS and authentication.
# PETLORD_API_HOST=127.0.0.1

# Internal maintainers only: inject local Lottery QA fixtures.
# VITE_PET_LORD_INTERNAL_FIXTURES=1
