# 桌面侧栏修正 · 0.1.12

2026-09-10，基线 `420fc1f`。问题复现截图中的导航使用 39px 高、12px 字、18px 图标的原生按钮；底部操作却是 36px 高、14px 字、16px 图标的居中组件库按钮。两个底部按钮各自设置 `margin-top: auto`，将剩余空间分成两段，使「打开 Studio」停在半空。

全部六个入口已统一为共享 Button，36px 高、14px 字、16px 图标、8px 圆角，图标起点一致。底部操作放入同一个靠底容器，按钮相隔4px。选择态采用共享暖色，当前位置由 `aria-current="page"` 表达；插件改用拼图图标。移除小于760px时将侧栏变成顶部导航的旧规则，保持桌面最小720×560窗口的一致布局。

实际键盘测试发现：在选中时把 Ant Design Button 从 text 切成 filled 会替换按钮节点、丢失焦点。最终固定 text variant，在共享控件层提供当前页背景。复测连续 Tab/Enter、Space、Shift+Tab 成功，焦点轮廓可见；弹窗 Escape 关闭后恢复触发按钮焦点。打开 Studio、隐藏设置正常。

开发版和最终安装包都验证了浅色/暗色 × 860×650、1280×800、720×560；六个入口尺寸、图标列、底部间隔一致，没有侧栏内容裁切或页面异常。已查看常规、最小窗口和键盘焦点截图。设计检查、三应用类型检查、共享控件与规范检查器11项测试、发行构建、签名和DMG校验均通过。

0.1.12 升级保留验证确认项目及修订、共享实体、宠物包与媒体、服务与运行设置、选中安装及外部配置保持完整。CLI 正常，同步没有重复导入。升级前完成完整备份；具体配置清单和备份记录保持私密。

DMG：`apps/desktop/release/PetLord-0.1.12-arm64.dmg`，192662010字节，SHA-256：`e69df68367fef5aa8656d95fc4736264338382fe9e4f70417366d21d2f2dc1f7`。

详细验证记录保持私密；检查使用独立配置，未改动外部接入状态。导航规范见 `docs/design-system.md`。
